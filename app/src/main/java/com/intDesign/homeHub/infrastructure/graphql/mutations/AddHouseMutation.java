// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL plugin from the GraphQL queries it found.
// It should not be modified by hand.
//
package com.intDesign.homeHub.infrastructure.graphql.mutations;

import com.apollographql.apollo.api.InputFieldMarshaller;
import com.apollographql.apollo.api.Mutation;
import com.apollographql.apollo.api.Operation;
import com.apollographql.apollo.api.OperationName;
import com.apollographql.apollo.api.ResponseField;
import com.apollographql.apollo.api.ResponseFieldMapper;
import com.apollographql.apollo.api.ResponseFieldMarshaller;
import com.apollographql.apollo.api.ResponseReader;
import com.apollographql.apollo.api.internal.UnmodifiableMapBuilder;
import com.apollographql.apollo.api.internal.Utils;
import com.apollographql.apollo.internal.QueryDocumentMinifier;
import com.intDesign.homeHub.infrastructure.graphql.types.HouseCreateViewModel;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

import java.util.Collections;
import java.util.LinkedHashMap;
import java.util.Map;

public final class AddHouseMutation implements Mutation<AddHouseMutation.Data, AddHouseMutation.Data, AddHouseMutation.Variables> {
    public static final String OPERATION_ID = "98446d241167c7c12be08545f3788ab5b4c13afc604272c3ca62eebf6e9a5c3c";

    public static final String QUERY_DOCUMENT = QueryDocumentMinifier.minify(
            "mutation AddHouse($house: HouseCreateViewModel!) {\n"
                    + "  house:house {\n"
                    + "    __typename\n"
                    + "    addHouse(house:$house) {\n"
                    + "      __typename\n"
                    + "      id\n"
                    + "    }\n"
                    + "  }\n"
                    + "}"
    );

    public static final OperationName OPERATION_NAME = () -> "AddHouse";

    private final AddHouseMutation.Variables variables;

    public AddHouseMutation(@NotNull HouseCreateViewModel house) {
        Utils.checkNotNull(house, "house == null");
        variables = new AddHouseMutation.Variables(house);
    }

    @NotNull
    @Override
    public String operationId() {
        return OPERATION_ID;
    }

    @Override
    public String queryDocument() {
        return QUERY_DOCUMENT;
    }

    @Override
    public AddHouseMutation.Data wrapData(AddHouseMutation.Data data) {
        return data;
    }

    @Override
    public AddHouseMutation.Variables variables() {
        return variables;
    }

    @Override
    public ResponseFieldMapper<AddHouseMutation.Data> responseFieldMapper() {
        return new Data.Mapper();
    }

    public static Builder builder() {
        return new Builder();
    }

    @NotNull
    @Override
    public OperationName name() {
        return OPERATION_NAME;
    }

    public static final class Builder {
        private @NotNull
        HouseCreateViewModel house;

        Builder() {
        }

        public Builder house(@NotNull HouseCreateViewModel house) {
            this.house = house;
            return this;
        }

        public AddHouseMutation build() {
            Utils.checkNotNull(house, "house == null");
            return new AddHouseMutation(house);
        }
    }

    public static final class Variables extends Operation.Variables {
        private final @NotNull
        HouseCreateViewModel house;

        private final transient Map<String, Object> valueMap = new LinkedHashMap<>();

        Variables(@NotNull HouseCreateViewModel house) {
            this.house = house;
            this.valueMap.put("house", house);
        }

        public @NotNull
        HouseCreateViewModel house() {
            return house;
        }

        @NotNull
        @Override
        public Map<String, Object> valueMap() {
            return Collections.unmodifiableMap(valueMap);
        }

        @Override
        public InputFieldMarshaller marshaller() {
            return writer -> writer.writeObject("house", house.marshaller());
        }
    }

    public static class Data implements Operation.Data {
        static final ResponseField[] $responseFields = {
                ResponseField.forObject("house", "house", null, true, Collections.emptyList())
        };

        final @Nullable
        House house;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public Data(@Nullable House house) {
            this.house = house;
        }

        public @Nullable
        House house() {
            return this.house;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> writer.writeObject($responseFields[0], house != null ? house.marshaller() : null);
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "Data{"
                        + "house=" + house
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof Data) {
                Data that = (Data) o;
                return ((this.house == null) ? (that.house == null) : this.house.equals(that.house));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= (house == null) ? 0 : house.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<Data> {
            final House.Mapper houseFieldMapper = new House.Mapper();

            @Override
            public Data map(ResponseReader reader) {
                final House house = reader.readObject($responseFields[0], houseFieldMapper::map);
                return new Data(house);
            }
        }
    }

    public static class House {
        static final ResponseField[] $responseFields = {
                ResponseField.forString("__typename", "__typename", null, false, Collections.emptyList()),
                ResponseField.forObject("addHouse", "addHouse", new UnmodifiableMapBuilder<String, Object>(1)
                        .put("house", new UnmodifiableMapBuilder<String, Object>(2)
                                .put("kind", "Variable")
                                .put("variableName", "house")
                                .build())
                        .build(), true, Collections.emptyList())
        };

        final @NotNull
        String __typename;

        final @Nullable
        AddHouse addHouse;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public House(@NotNull String __typename, @Nullable AddHouse addHouse) {
            this.__typename = Utils.checkNotNull(__typename, "__typename == null");
            this.addHouse = addHouse;
        }

        public @NotNull
        String __typename() {
            return this.__typename;
        }

        public @Nullable
        AddHouse addHouse() {
            return this.addHouse;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> {
                writer.writeString($responseFields[0], __typename);
                writer.writeObject($responseFields[1], addHouse != null ? addHouse.marshaller() : null);
            };
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "House{"
                        + "__typename=" + __typename + ", "
                        + "addHouse=" + addHouse
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof House) {
                House that = (House) o;
                return this.__typename.equals(that.__typename)
                        && ((this.addHouse == null) ? (that.addHouse == null) : this.addHouse.equals(that.addHouse));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= __typename.hashCode();
                h *= 1000003;
                h ^= (addHouse == null) ? 0 : addHouse.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<House> {
            final AddHouse.Mapper addHouseFieldMapper = new AddHouse.Mapper();

            @Override
            public House map(ResponseReader reader) {
                final String __typename = reader.readString($responseFields[0]);
                final AddHouse addHouse = reader.readObject($responseFields[1], addHouseFieldMapper::map);
                return new House(__typename, addHouse);
            }
        }
    }

    public static class AddHouse {
        static final ResponseField[] $responseFields = {
                ResponseField.forString("__typename", "__typename", null, false, Collections.emptyList()),
                ResponseField.forString("id", "id", null, true, Collections.emptyList())
        };

        final @NotNull
        String __typename;

        final @Nullable
        String id;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public AddHouse(@NotNull String __typename, @Nullable String id) {
            this.__typename = Utils.checkNotNull(__typename, "__typename == null");
            this.id = id;
        }

        public @NotNull
        String __typename() {
            return this.__typename;
        }

        /**
         * House ID
         */
        public @Nullable
        String id() {
            return this.id;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> {
                writer.writeString($responseFields[0], __typename);
                writer.writeString($responseFields[1], id);
            };
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "AddHouse{"
                        + "__typename=" + __typename + ", "
                        + "id=" + id
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof AddHouse) {
                AddHouse that = (AddHouse) o;
                return this.__typename.equals(that.__typename)
                        && ((this.id == null) ? (that.id == null) : this.id.equals(that.id));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= __typename.hashCode();
                h *= 1000003;
                h ^= (id == null) ? 0 : id.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<AddHouse> {
            @Override
            public AddHouse map(ResponseReader reader) {
                final String __typename = reader.readString($responseFields[0]);
                final String id = reader.readString($responseFields[1]);
                return new AddHouse(__typename, id);
            }
        }
    }
}
