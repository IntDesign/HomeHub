// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL plugin from the GraphQL queries it found.
// It should not be modified by hand.
//
package com.intDesign.homeHub.infrastructure.graphql.mutations;

import com.apollographql.apollo.api.InputFieldMarshaller;
import com.apollographql.apollo.api.Mutation;
import com.apollographql.apollo.api.Operation;
import com.apollographql.apollo.api.OperationName;
import com.apollographql.apollo.api.ResponseField;
import com.apollographql.apollo.api.ResponseFieldMapper;
import com.apollographql.apollo.api.ResponseFieldMarshaller;
import com.apollographql.apollo.api.ResponseReader;
import com.apollographql.apollo.api.internal.UnmodifiableMapBuilder;
import com.apollographql.apollo.api.internal.Utils;
import com.apollographql.apollo.internal.QueryDocumentMinifier;
import com.intDesign.homeHub.infrastructure.graphql.types.MaterialInformationCreateViewModel;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

import java.util.Collections;
import java.util.LinkedHashMap;
import java.util.Map;

public final class AddMaterialInfoMutation implements Mutation<AddMaterialInfoMutation.Data, AddMaterialInfoMutation.Data, AddMaterialInfoMutation.Variables> {
    public static final String OPERATION_ID = "296aef6bb0787236bf6dbd215858db7bc36a2efe73b924bec647a2a69af0ab9d";

    public static final String QUERY_DOCUMENT = QueryDocumentMinifier.minify(
            "mutation AddMaterialInfo($info:MaterialInformationCreateViewModel!) {\n"
                    + "  materialInfo:materialInfo {\n"
                    + "    __typename\n"
                    + "    addMaterialInfo(materialInfo:$info) {\n"
                    + "      __typename\n"
                    + "      id\n"
                    + "    }\n"
                    + "  }\n"
                    + "}"
    );

    public static final OperationName OPERATION_NAME = () -> "AddMaterialInfo";

    private final AddMaterialInfoMutation.Variables variables;

    public AddMaterialInfoMutation(@NotNull MaterialInformationCreateViewModel info) {
        Utils.checkNotNull(info, "info == null");
        variables = new AddMaterialInfoMutation.Variables(info);
    }

    @NotNull
    @Override
    public String operationId() {
        return OPERATION_ID;
    }

    @Override
    public String queryDocument() {
        return QUERY_DOCUMENT;
    }

    @Override
    public AddMaterialInfoMutation.Data wrapData(AddMaterialInfoMutation.Data data) {
        return data;
    }

    @Override
    public AddMaterialInfoMutation.Variables variables() {
        return variables;
    }

    @Override
    public ResponseFieldMapper<AddMaterialInfoMutation.Data> responseFieldMapper() {
        return new Data.Mapper();
    }

    public static Builder builder() {
        return new Builder();
    }

    @NotNull
    @Override
    public OperationName name() {
        return OPERATION_NAME;
    }

    public static final class Builder {
        private @NotNull
        MaterialInformationCreateViewModel info;

        Builder() {
        }

        public Builder info(@NotNull MaterialInformationCreateViewModel info) {
            this.info = info;
            return this;
        }

        public AddMaterialInfoMutation build() {
            Utils.checkNotNull(info, "info == null");
            return new AddMaterialInfoMutation(info);
        }
    }

    public static final class Variables extends Operation.Variables {
        private final @NotNull
        MaterialInformationCreateViewModel info;

        private final transient Map<String, Object> valueMap = new LinkedHashMap<>();

        Variables(@NotNull MaterialInformationCreateViewModel info) {
            this.info = info;
            this.valueMap.put("info", info);
        }

        public @NotNull
        MaterialInformationCreateViewModel info() {
            return info;
        }

        @NotNull
        @Override
        public Map<String, Object> valueMap() {
            return Collections.unmodifiableMap(valueMap);
        }

        @Override
        public InputFieldMarshaller marshaller() {
            return writer -> writer.writeObject("info", info.marshaller());
        }
    }

    public static class Data implements Operation.Data {
        static final ResponseField[] $responseFields = {
                ResponseField.forObject("materialInfo", "materialInfo", null, true, Collections.emptyList())
        };

        final @Nullable
        MaterialInfo materialInfo;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public Data(@Nullable MaterialInfo materialInfo) {
            this.materialInfo = materialInfo;
        }

        public @Nullable
        MaterialInfo materialInfo() {
            return this.materialInfo;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> writer.writeObject($responseFields[0], materialInfo != null ? materialInfo.marshaller() : null);
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "Data{"
                        + "materialInfo=" + materialInfo
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof Data) {
                Data that = (Data) o;
                return ((this.materialInfo == null) ? (that.materialInfo == null) : this.materialInfo.equals(that.materialInfo));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= (materialInfo == null) ? 0 : materialInfo.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<Data> {
            final MaterialInfo.Mapper materialInfoFieldMapper = new MaterialInfo.Mapper();

            @Override
            public Data map(ResponseReader reader) {
                final MaterialInfo materialInfo = reader.readObject($responseFields[0], materialInfoFieldMapper::map);
                return new Data(materialInfo);
            }
        }
    }

    public static class MaterialInfo {
        static final ResponseField[] $responseFields = {
                ResponseField.forString("__typename", "__typename", null, false, Collections.emptyList()),
                ResponseField.forObject("addMaterialInfo", "addMaterialInfo", new UnmodifiableMapBuilder<String, Object>(1)
                        .put("materialInfo", new UnmodifiableMapBuilder<String, Object>(2)
                                .put("kind", "Variable")
                                .put("variableName", "info")
                                .build())
                        .build(), true, Collections.emptyList())
        };

        final @NotNull
        String __typename;

        final @Nullable
        AddMaterialInfo addMaterialInfo;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public MaterialInfo(@NotNull String __typename, @Nullable AddMaterialInfo addMaterialInfo) {
            this.__typename = Utils.checkNotNull(__typename, "__typename == null");
            this.addMaterialInfo = addMaterialInfo;
        }

        public @NotNull
        String __typename() {
            return this.__typename;
        }

        public @Nullable
        AddMaterialInfo addMaterialInfo() {
            return this.addMaterialInfo;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> {
                writer.writeString($responseFields[0], __typename);
                writer.writeObject($responseFields[1], addMaterialInfo != null ? addMaterialInfo.marshaller() : null);
            };
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "MaterialInfo{"
                        + "__typename=" + __typename + ", "
                        + "addMaterialInfo=" + addMaterialInfo
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof MaterialInfo) {
                MaterialInfo that = (MaterialInfo) o;
                return this.__typename.equals(that.__typename)
                        && ((this.addMaterialInfo == null) ? (that.addMaterialInfo == null) : this.addMaterialInfo.equals(that.addMaterialInfo));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= __typename.hashCode();
                h *= 1000003;
                h ^= (addMaterialInfo == null) ? 0 : addMaterialInfo.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<MaterialInfo> {
            final AddMaterialInfo.Mapper addMaterialInfoFieldMapper = new AddMaterialInfo.Mapper();

            @Override
            public MaterialInfo map(ResponseReader reader) {
                final String __typename = reader.readString($responseFields[0]);
                final AddMaterialInfo addMaterialInfo = reader.readObject($responseFields[1], addMaterialInfoFieldMapper::map);
                return new MaterialInfo(__typename, addMaterialInfo);
            }
        }
    }

    public static class AddMaterialInfo {
        static final ResponseField[] $responseFields = {
                ResponseField.forString("__typename", "__typename", null, false, Collections.emptyList()),
                ResponseField.forString("id", "id", null, true, Collections.emptyList())
        };

        final @NotNull
        String __typename;

        final @Nullable
        String id;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public AddMaterialInfo(@NotNull String __typename, @Nullable String id) {
            this.__typename = Utils.checkNotNull(__typename, "__typename == null");
            this.id = id;
        }

        public @NotNull
        String __typename() {
            return this.__typename;
        }

        /**
         * Material Information ID
         */
        public @Nullable
        String id() {
            return this.id;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> {
                writer.writeString($responseFields[0], __typename);
                writer.writeString($responseFields[1], id);
            };
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "AddMaterialInfo{"
                        + "__typename=" + __typename + ", "
                        + "id=" + id
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof AddMaterialInfo) {
                AddMaterialInfo that = (AddMaterialInfo) o;
                return this.__typename.equals(that.__typename)
                        && ((this.id == null) ? (that.id == null) : this.id.equals(that.id));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= __typename.hashCode();
                h *= 1000003;
                h ^= (id == null) ? 0 : id.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<AddMaterialInfo> {
            @Override
            public AddMaterialInfo map(ResponseReader reader) {
                final String __typename = reader.readString($responseFields[0]);
                final String id = reader.readString($responseFields[1]);
                return new AddMaterialInfo(__typename, id);
            }
        }
    }
}
