// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL plugin from the GraphQL queries it found.
// It should not be modified by hand.
//
package com.intDesign.homeHub.infrastructure.graphql.mutations;

import com.apollographql.apollo.api.InputFieldMarshaller;
import com.apollographql.apollo.api.Mutation;
import com.apollographql.apollo.api.Operation;
import com.apollographql.apollo.api.OperationName;
import com.apollographql.apollo.api.ResponseField;
import com.apollographql.apollo.api.ResponseFieldMapper;
import com.apollographql.apollo.api.ResponseFieldMarshaller;
import com.apollographql.apollo.api.ResponseReader;
import com.apollographql.apollo.api.internal.UnmodifiableMapBuilder;
import com.apollographql.apollo.api.internal.Utils;
import com.apollographql.apollo.internal.QueryDocumentMinifier;
import com.intDesign.homeHub.infrastructure.graphql.types.RoomCreateViewModel;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

import java.util.Collections;
import java.util.LinkedHashMap;
import java.util.Map;

public final class AddRoomMutation implements Mutation<AddRoomMutation.Data, AddRoomMutation.Data, AddRoomMutation.Variables> {
    public static final String OPERATION_ID = "d41edb8cd73b76cd2a783f1af0d9272ac3772e2fee368b5c479779311307444c";

    public static final String QUERY_DOCUMENT = QueryDocumentMinifier.minify(
            "mutation AddRoom($room:RoomCreateViewModel!) {\n"
                    + "  room:room {\n"
                    + "    __typename\n"
                    + "    addRoom(room:$room) {\n"
                    + "      __typename\n"
                    + "      id\n"
                    + "    }\n"
                    + "  }\n"
                    + "}"
    );

    public static final OperationName OPERATION_NAME = () -> "AddRoom";

    private final AddRoomMutation.Variables variables;

    public AddRoomMutation(@NotNull RoomCreateViewModel room) {
        Utils.checkNotNull(room, "room == null");
        variables = new AddRoomMutation.Variables(room);
    }

    @NotNull
    @Override
    public String operationId() {
        return OPERATION_ID;
    }

    @Override
    public String queryDocument() {
        return QUERY_DOCUMENT;
    }

    @Override
    public AddRoomMutation.Data wrapData(AddRoomMutation.Data data) {
        return data;
    }

    @Override
    public AddRoomMutation.Variables variables() {
        return variables;
    }

    @Override
    public ResponseFieldMapper<AddRoomMutation.Data> responseFieldMapper() {
        return new Data.Mapper();
    }

    public static Builder builder() {
        return new Builder();
    }

    @NotNull
    @Override
    public OperationName name() {
        return OPERATION_NAME;
    }

    public static final class Builder {
        private @NotNull
        RoomCreateViewModel room;

        Builder() {
        }

        public Builder room(@NotNull RoomCreateViewModel room) {
            this.room = room;
            return this;
        }

        public AddRoomMutation build() {
            Utils.checkNotNull(room, "room == null");
            return new AddRoomMutation(room);
        }
    }

    public static final class Variables extends Operation.Variables {
        private final @NotNull
        RoomCreateViewModel room;

        private final transient Map<String, Object> valueMap = new LinkedHashMap<>();

        Variables(@NotNull RoomCreateViewModel room) {
            this.room = room;
            this.valueMap.put("room", room);
        }

        public @NotNull
        RoomCreateViewModel room() {
            return room;
        }

        @NotNull
        @Override
        public Map<String, Object> valueMap() {
            return Collections.unmodifiableMap(valueMap);
        }

        @Override
        public InputFieldMarshaller marshaller() {
            return writer -> writer.writeObject("room", room.marshaller());
        }
    }

    public static class Data implements Operation.Data {
        static final ResponseField[] $responseFields = {
                ResponseField.forObject("room", "room", null, true, Collections.emptyList())
        };

        final @Nullable
        Room room;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public Data(@Nullable Room room) {
            this.room = room;
        }

        public @Nullable
        Room room() {
            return this.room;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> writer.writeObject($responseFields[0], room != null ? room.marshaller() : null);
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "Data{"
                        + "room=" + room
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof Data) {
                Data that = (Data) o;
                return ((this.room == null) ? (that.room == null) : this.room.equals(that.room));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= (room == null) ? 0 : room.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<Data> {
            final Room.Mapper roomFieldMapper = new Room.Mapper();

            @Override
            public Data map(ResponseReader reader) {
                final Room room = reader.readObject($responseFields[0], roomFieldMapper::map);
                return new Data(room);
            }
        }
    }

    public static class Room {
        static final ResponseField[] $responseFields = {
                ResponseField.forString("__typename", "__typename", null, false, Collections.emptyList()),
                ResponseField.forObject("addRoom", "addRoom", new UnmodifiableMapBuilder<String, Object>(1)
                        .put("room", new UnmodifiableMapBuilder<String, Object>(2)
                                .put("kind", "Variable")
                                .put("variableName", "room")
                                .build())
                        .build(), true, Collections.emptyList())
        };

        final @NotNull
        String __typename;

        final @Nullable
        AddRoom addRoom;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public Room(@NotNull String __typename, @Nullable AddRoom addRoom) {
            this.__typename = Utils.checkNotNull(__typename, "__typename == null");
            this.addRoom = addRoom;
        }

        public @NotNull
        String __typename() {
            return this.__typename;
        }

        public @Nullable
        AddRoom addRoom() {
            return this.addRoom;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> {
                writer.writeString($responseFields[0], __typename);
                writer.writeObject($responseFields[1], addRoom != null ? addRoom.marshaller() : null);
            };
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "Room{"
                        + "__typename=" + __typename + ", "
                        + "addRoom=" + addRoom
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof Room) {
                Room that = (Room) o;
                return this.__typename.equals(that.__typename)
                        && ((this.addRoom == null) ? (that.addRoom == null) : this.addRoom.equals(that.addRoom));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= __typename.hashCode();
                h *= 1000003;
                h ^= (addRoom == null) ? 0 : addRoom.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<Room> {
            final AddRoom.Mapper addRoomFieldMapper = new AddRoom.Mapper();

            @Override
            public Room map(ResponseReader reader) {
                final String __typename = reader.readString($responseFields[0]);
                final AddRoom addRoom = reader.readObject($responseFields[1], addRoomFieldMapper::map);
                return new Room(__typename, addRoom);
            }
        }
    }

    public static class AddRoom {
        static final ResponseField[] $responseFields = {
                ResponseField.forString("__typename", "__typename", null, false, Collections.emptyList()),
                ResponseField.forString("id", "id", null, true, Collections.emptyList())
        };

        final @NotNull
        String __typename;

        final @Nullable
        String id;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public AddRoom(@NotNull String __typename, @Nullable String id) {
            this.__typename = Utils.checkNotNull(__typename, "__typename == null");
            this.id = id;
        }

        public @NotNull
        String __typename() {
            return this.__typename;
        }

        /**
         * Room ID
         */
        public @Nullable
        String id() {
            return this.id;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return writer -> {
                writer.writeString($responseFields[0], __typename);
                writer.writeString($responseFields[1], id);
            };
        }

        @NotNull
        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "AddRoom{"
                        + "__typename=" + __typename + ", "
                        + "id=" + id
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof AddRoom) {
                AddRoom that = (AddRoom) o;
                return this.__typename.equals(that.__typename)
                        && ((this.id == null) ? (that.id == null) : this.id.equals(that.id));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= __typename.hashCode();
                h *= 1000003;
                h ^= (id == null) ? 0 : id.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<AddRoom> {
            @Override
            public AddRoom map(ResponseReader reader) {
                final String __typename = reader.readString($responseFields[0]);
                final String id = reader.readString($responseFields[1]);
                return new AddRoom(__typename, id);
            }
        }
    }
}
