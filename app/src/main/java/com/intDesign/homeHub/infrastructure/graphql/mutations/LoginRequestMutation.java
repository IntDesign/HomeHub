// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL plugin from the GraphQL queries it found.
// It should not be modified by hand.
//
package com.intDesign.homeHub.infrastructure.graphql.mutations;

import com.apollographql.apollo.api.InputFieldMarshaller;
import com.apollographql.apollo.api.InputFieldWriter;
import com.apollographql.apollo.api.Mutation;
import com.apollographql.apollo.api.Operation;
import com.apollographql.apollo.api.OperationName;
import com.apollographql.apollo.api.ResponseField;
import com.apollographql.apollo.api.ResponseFieldMapper;
import com.apollographql.apollo.api.ResponseFieldMarshaller;
import com.apollographql.apollo.api.ResponseReader;
import com.apollographql.apollo.api.ResponseWriter;
import com.apollographql.apollo.api.internal.UnmodifiableMapBuilder;
import com.apollographql.apollo.api.internal.Utils;
import com.apollographql.apollo.internal.QueryDocumentMinifier;
import com.intDesign.homeHub.infrastructure.graphql.types.LoginCreateViewModel;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

import java.io.IOException;
import java.util.Collections;
import java.util.LinkedHashMap;
import java.util.Map;

public final class LoginRequestMutation implements Mutation<LoginRequestMutation.Data, LoginRequestMutation.Data, LoginRequestMutation.Variables> {
    public static final String OPERATION_ID = "e00366840df6bb53bfcadee360c9a7888e320f03e79c188105f32ac74edbaa84";

    public static final String QUERY_DOCUMENT = QueryDocumentMinifier.minify(
            "mutation loginRequest($request: LoginCreateViewModel!) {\n"
                    + "  login {\n"
                    + "    __typename\n"
                    + "    sendLogin(request: $request) {\n"
                    + "      __typename\n"
                    + "      isSuccessful\n"
                    + "      userId\n"
                    + "      error\n"
                    + "    }\n"
                    + "  }\n"
                    + "}"
    );

    public static final OperationName OPERATION_NAME = new OperationName() {
        @Override
        public String name() {
            return "loginRequest";
        }
    };

    private final LoginRequestMutation.Variables variables;

    public LoginRequestMutation(@NotNull LoginCreateViewModel request) {
        Utils.checkNotNull(request, "request == null");
        variables = new LoginRequestMutation.Variables(request);
    }

    @Override
    public String operationId() {
        return OPERATION_ID;
    }

    @Override
    public String queryDocument() {
        return QUERY_DOCUMENT;
    }

    @Override
    public LoginRequestMutation.Data wrapData(LoginRequestMutation.Data data) {
        return data;
    }

    @Override
    public LoginRequestMutation.Variables variables() {
        return variables;
    }

    @Override
    public ResponseFieldMapper<LoginRequestMutation.Data> responseFieldMapper() {
        return new Data.Mapper();
    }

    public static Builder builder() {
        return new Builder();
    }

    @Override
    public OperationName name() {
        return OPERATION_NAME;
    }

    public static final class Builder {
        private @NotNull
        LoginCreateViewModel request;

        Builder() {
        }

        public Builder request(@NotNull LoginCreateViewModel request) {
            this.request = request;
            return this;
        }

        public LoginRequestMutation build() {
            Utils.checkNotNull(request, "request == null");
            return new LoginRequestMutation(request);
        }
    }

    public static final class Variables extends Operation.Variables {
        private final @NotNull
        LoginCreateViewModel request;

        private final transient Map<String, Object> valueMap = new LinkedHashMap<>();

        Variables(@NotNull LoginCreateViewModel request) {
            this.request = request;
            this.valueMap.put("request", request);
        }

        public @NotNull
        LoginCreateViewModel request() {
            return request;
        }

        @Override
        public Map<String, Object> valueMap() {
            return Collections.unmodifiableMap(valueMap);
        }

        @Override
        public InputFieldMarshaller marshaller() {
            return new InputFieldMarshaller() {
                @Override
                public void marshal(InputFieldWriter writer) throws IOException {
                    writer.writeObject("request", request.marshaller());
                }
            };
        }
    }

    public static class Data implements Operation.Data {
        static final ResponseField[] $responseFields = {
                ResponseField.forObject("login", "login", null, true, Collections.<ResponseField.Condition>emptyList())
        };

        final @Nullable
        Login login;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public Data(@Nullable Login login) {
            this.login = login;
        }

        public @Nullable
        Login login() {
            return this.login;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return new ResponseFieldMarshaller() {
                @Override
                public void marshal(ResponseWriter writer) {
                    writer.writeObject($responseFields[0], login != null ? login.marshaller() : null);
                }
            };
        }

        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "Data{"
                        + "login=" + login
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof Data) {
                Data that = (Data) o;
                return ((this.login == null) ? (that.login == null) : this.login.equals(that.login));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= (login == null) ? 0 : login.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<Data> {
            final Login.Mapper loginFieldMapper = new Login.Mapper();

            @Override
            public Data map(ResponseReader reader) {
                final Login login = reader.readObject($responseFields[0], new ResponseReader.ObjectReader<Login>() {
                    @Override
                    public Login read(ResponseReader reader) {
                        return loginFieldMapper.map(reader);
                    }
                });
                return new Data(login);
            }
        }
    }

    public static class Login {
        static final ResponseField[] $responseFields = {
                ResponseField.forString("__typename", "__typename", null, false, Collections.<ResponseField.Condition>emptyList()),
                ResponseField.forObject("sendLogin", "sendLogin", new UnmodifiableMapBuilder<String, Object>(1)
                        .put("request", new UnmodifiableMapBuilder<String, Object>(2)
                                .put("kind", "Variable")
                                .put("variableName", "request")
                                .build())
                        .build(), true, Collections.<ResponseField.Condition>emptyList())
        };

        final @NotNull
        String __typename;

        final @Nullable
        SendLogin sendLogin;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public Login(@NotNull String __typename, @Nullable SendLogin sendLogin) {
            this.__typename = Utils.checkNotNull(__typename, "__typename == null");
            this.sendLogin = sendLogin;
        }

        public @NotNull
        String __typename() {
            return this.__typename;
        }

        public @Nullable
        SendLogin sendLogin() {
            return this.sendLogin;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return new ResponseFieldMarshaller() {
                @Override
                public void marshal(ResponseWriter writer) {
                    writer.writeString($responseFields[0], __typename);
                    writer.writeObject($responseFields[1], sendLogin != null ? sendLogin.marshaller() : null);
                }
            };
        }

        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "Login{"
                        + "__typename=" + __typename + ", "
                        + "sendLogin=" + sendLogin
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof Login) {
                Login that = (Login) o;
                return this.__typename.equals(that.__typename)
                        && ((this.sendLogin == null) ? (that.sendLogin == null) : this.sendLogin.equals(that.sendLogin));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= __typename.hashCode();
                h *= 1000003;
                h ^= (sendLogin == null) ? 0 : sendLogin.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<Login> {
            final SendLogin.Mapper sendLoginFieldMapper = new SendLogin.Mapper();

            @Override
            public Login map(ResponseReader reader) {
                final String __typename = reader.readString($responseFields[0]);
                final SendLogin sendLogin = reader.readObject($responseFields[1], new ResponseReader.ObjectReader<SendLogin>() {
                    @Override
                    public SendLogin read(ResponseReader reader) {
                        return sendLoginFieldMapper.map(reader);
                    }
                });
                return new Login(__typename, sendLogin);
            }
        }
    }

    public static class SendLogin {
        static final ResponseField[] $responseFields = {
                ResponseField.forString("__typename", "__typename", null, false, Collections.<ResponseField.Condition>emptyList()),
                ResponseField.forBoolean("isSuccessful", "isSuccessful", null, true, Collections.<ResponseField.Condition>emptyList()),
                ResponseField.forString("userId", "userId", null, true, Collections.<ResponseField.Condition>emptyList()),
                ResponseField.forString("error", "error", null, true, Collections.<ResponseField.Condition>emptyList())
        };

        final @NotNull
        String __typename;

        final @Nullable
        Boolean isSuccessful;

        final @Nullable
        String userId;

        final @Nullable
        String error;

        private transient volatile String $toString;

        private transient volatile int $hashCode;

        private transient volatile boolean $hashCodeMemoized;

        public SendLogin(@NotNull String __typename, @Nullable Boolean isSuccessful,
                         @Nullable String userId, @Nullable String error) {
            this.__typename = Utils.checkNotNull(__typename, "__typename == null");
            this.isSuccessful = isSuccessful;
            this.userId = userId;
            this.error = error;
        }

        public @NotNull
        String __typename() {
            return this.__typename;
        }

        /**
         * User ID
         */
        public @Nullable
        Boolean isSuccessful() {
            return this.isSuccessful;
        }

        /**
         * User ID
         */
        public @Nullable
        String userId() {
            return this.userId;
        }

        /**
         * Fail Reason
         */
        public @Nullable
        String error() {
            return this.error;
        }

        @SuppressWarnings("unchecked")
        public ResponseFieldMarshaller marshaller() {
            return new ResponseFieldMarshaller() {
                @Override
                public void marshal(ResponseWriter writer) {
                    writer.writeString($responseFields[0], __typename);
                    writer.writeBoolean($responseFields[1], isSuccessful);
                    writer.writeString($responseFields[2], userId);
                    writer.writeString($responseFields[3], error);
                }
            };
        }

        @Override
        public String toString() {
            if ($toString == null) {
                $toString = "SendLogin{"
                        + "__typename=" + __typename + ", "
                        + "isSuccessful=" + isSuccessful + ", "
                        + "userId=" + userId + ", "
                        + "error=" + error
                        + "}";
            }
            return $toString;
        }

        @Override
        public boolean equals(Object o) {
            if (o == this) {
                return true;
            }
            if (o instanceof SendLogin) {
                SendLogin that = (SendLogin) o;
                return this.__typename.equals(that.__typename)
                        && ((this.isSuccessful == null) ? (that.isSuccessful == null) : this.isSuccessful.equals(that.isSuccessful))
                        && ((this.userId == null) ? (that.userId == null) : this.userId.equals(that.userId))
                        && ((this.error == null) ? (that.error == null) : this.error.equals(that.error));
            }
            return false;
        }

        @Override
        public int hashCode() {
            if (!$hashCodeMemoized) {
                int h = 1;
                h *= 1000003;
                h ^= __typename.hashCode();
                h *= 1000003;
                h ^= (isSuccessful == null) ? 0 : isSuccessful.hashCode();
                h *= 1000003;
                h ^= (userId == null) ? 0 : userId.hashCode();
                h *= 1000003;
                h ^= (error == null) ? 0 : error.hashCode();
                $hashCode = h;
                $hashCodeMemoized = true;
            }
            return $hashCode;
        }

        public static final class Mapper implements ResponseFieldMapper<SendLogin> {
            @Override
            public SendLogin map(ResponseReader reader) {
                final String __typename = reader.readString($responseFields[0]);
                final Boolean isSuccessful = reader.readBoolean($responseFields[1]);
                final String userId = reader.readString($responseFields[2]);
                final String error = reader.readString($responseFields[3]);
                return new SendLogin(__typename, isSuccessful, userId, error);
            }
        }
    }
}
